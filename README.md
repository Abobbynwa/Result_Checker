# Result Checker

This project contains a Flask backend and a React frontend for managing student results. The frontend communicates with the backend API to authenticate users, upload CSV files, and display results.

